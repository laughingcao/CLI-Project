# frozen_string_literal: true

RSpec.describe Best50Bars do
  it "has a version number" do
    expect(Best50Bars::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
